name: Use GitHub Models
on:
  workflow_dispatch:

permissions:
  models: read # allow calling the Models inference API

jobs:
  call-llm:
    runs-on: ubuntu-latest
    steps:
      - name: Ask a model
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sS https://models.github.ai/inference/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -d '{
              "model": "openai/gpt-5",
              "messages": [
                {"role": "system", "content": "You are a helpful assistant for Go development."},
                {"role": "user", "content": "Explain Go interfaces and their benefits in 3 sentences."}
              ],
              "max_tokens": 300
            }'
